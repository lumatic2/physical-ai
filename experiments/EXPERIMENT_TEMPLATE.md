# {NN}-{slug} — {한 줄 제목}

> `experiments/{NN}-{slug}/README.md` — 실험은 *가설·방법·결과·통찰* 4섹션.
> 통찰이 비었으면 실험이 *안 끝난* 것. mock + real 둘 다 박제 권장.

## 1. 가설 (Hypothesis)

{무엇이 사실이면 이 실험이 의미 있는가, 한 문장. 반증 가능해야 함.}

예: "검증자가 양방향이면(모델→하네스 + 하네스→모델 피드백), 모델은 *기존 답을 외과적으로 수정* 한다 — 새로 다 쓰지 않고."

## 2. 방법 (Method)

### 셋업
- 모델: {claude / gpt / mock}
- 데이터: {셋 이름·크기·출처}
- 하네스 구성: {어떤 모듈 import, 어떤 cap}

### 시나리오 (mock 먼저, real 다음)
- V0/M1: {mock — 통제된 입력}
- V1/M2: {real — 실제 모델 호출}
- ...

### 측정 metric
- {정확도 / 비용 / retry 횟수 / ...}

## 3. 결과 (Results)

### 데이터
| Run | Verdict | Cost | Retries | 비고 |
|-----|---------|------|---------|------|
| V0 | | | | |

### 박제 위치
- `verify/` 폴더에 raw 출력 보존 (재현성 P7)

## 4. 통찰 (Insights)

### 무엇을 알아냈나
- {한 줄 명제 1}
- {한 줄 명제 2}

### 가설은 통과했나?
- [ ] PASS — 어떤 근거로
- [ ] FAIL — 무엇이 어긋났나, 가설 수정

### 정의에 반영
- `docs/{...}` 에 v{n.m} 로 갱신, 또는 ADR 발행

### 다음 실험 후보
- {여기서 발견한 새 질문}
