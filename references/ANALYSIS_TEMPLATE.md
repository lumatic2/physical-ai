# {handle}/{repo} 분석

> `references/{handle}-{repo}/ANALYSIS.md` — 클론 옆에 둠. 시간 박스: 레포당 90분.
> 5섹션 다 채우기 전엔 *통찰 보고 / 정의 갱신 금지* (그래야 의도 끌어옴).

## 1. 한 줄 요약 / 무엇을 하는 하네스인가

{한 문장. 외부 정의 어휘(agent / eval / context / guardrail)로 분류 시도}

## 2. 디렉터리 지도 (핵심 폴더만)

```
{repo}/
├── {핵심 폴더 1}/    # 무엇인지 한 줄
├── {핵심 폴더 2}/    # 무엇인지 한 줄
└── ...
```

(루트 stray 파일·자명한 폴더는 생략)

## 3. 하네스 구성요소 매핑

| 계층 | 위치 (파일:줄) | 내용 |
|------|---------------|------|
| L0 Model | | |
| L1 Executor | | |
| L2 Plugins/tools | | |
| L3 Self-diag | | |
| Guardrails | | |

## 4. 인상 깊은 코드/패턴 (파일 경로 + 줄 번호 필수)

- `{path}:L{N}` — {왜 인상 깊은가, 한 단락}
- `{path}:L{N}` — {...}

## 5. 내 정의에 어떻게 반영할 것인가

- {기존 정의의 어떤 부분을 강화/수정/추가하는가}
- {반영하지 않는다면 왜}
- 관련 ADR: `docs/adr/{n}-{title}.md`

---

## 메타
- 수집일: YYYY-MM-DD
- 영상/아티클: {링크}
- 소요 시간: {분}
- 다음에 볼 레포 후보: {여기서 발견한 cross-reference}
